# HeartMirror 项目 Railway 构建配置
# 指定从 backend 目录构建 Python FastAPI 服务

[phases.setup]
nixPkgs = ['python311']

[phases.install]
cmds = ['cd backend && pip install -r requirements.txt']

[start]
cmd = 'cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT'